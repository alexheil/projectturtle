<%= @game.title %>

<%= link_to "create playlist", new_game_playlist_path(@game) %></br>

<% @playlists.each do |playlist| %>
  <%= link_to playlist.title, game_playlist_path(@game, playlist) %>
<% end %>